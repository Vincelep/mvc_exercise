<%= form_for item, url: administration_item_url(item.id), html: {class: ""} do |f| %>
	<tr>
		<td> <%= item.original_price %></td>
		<td> <%= item.discount_percentage %></td>
		<td> <%= item.price %></td>
	</tr>
<% end %>